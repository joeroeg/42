#include <unistd.h>
#include <stdio.h>

int main(void)
{
	const char key[] = "ak48fnsdoepr85yt";
	const char *salt = "";
	char *crypt(const char *key, const char *salt);
	printf("%c", char *salt);
	return (0);
}	
