{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/course/libft/a.out",
        "args": [],
        "cwd": "${workspaceFolder}",
    }
    ]
}